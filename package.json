{
  "name": "smart-land-assistant-root",
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "start": "node server/index.js"
  }
}